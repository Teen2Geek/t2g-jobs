h1 Internships for Teens
.internships(ng-controller='internshipCtrl')
 .internship(ng-repeat='job in jobs | filter:search', class='{{slug}} {{job.lv}}')
  h3.job-name 
   | {{job.name}} - 
   a(href='{{job.site}}') {{job.company}}
  .info
   p.location
    i(class='fa fa-globe')
    | {{job.location}}
   a(href="{{ 'mailto:' + job.contact + '?subject=' + job.name + '%20Inquiry%20-%20T2G' }}")
    i.fa.fa-envelope
    | Apply - {{job.contact}}

